// plugins/iconify-icon.client.js

// Import the defineNuxtPlugin helper
import { defineNuxtPlugin } from '#app'
// Import the iconify-icon package
import 'iconify-icon'

export default defineNuxtPlugin(() => {
  // Plugin code (if needed, though it may not be necessary for just importing)
})
